const initialState = {
  language: 'english',
};

const reducer = (state = initialState.addedIds, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
